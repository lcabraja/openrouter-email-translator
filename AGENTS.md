# Agent instructions

Use **bun** for JavaScript and TypeScript tooling in this workspace. Prefer `bun` over `node`, `npm`, `yarn`, or `pnpm` for installing packages, running scripts, and invoking CLIs (for example `bun install`, `bun run`, `bun test`, `bunx`).
